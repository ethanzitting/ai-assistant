// Stamped onto archived_files.ocr_model on success, so a NULL ocr_text can be told apart from an
// OCR that never ran. Mirrors VISION_MODEL and EMBEDDING_MODEL_TAG.
export const OCR_MODEL = "mistral-ocr-latest";
