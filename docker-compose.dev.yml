services:
  agent:
    volumes:
      - ./agent/src:/app/src
      - ./agent/deno.json:/app/deno.json
    command: ["deno", "run", "--watch", "--allow-net", "--allow-env", "--allow-read", "src/main.ts"]
